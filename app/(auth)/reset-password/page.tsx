import { ResetPasswordForm } from "./reset-form";

export const metadata = { title: "Set new password · Veridigits" };

export default function ResetPasswordPage() {
  return <ResetPasswordForm />;
}
